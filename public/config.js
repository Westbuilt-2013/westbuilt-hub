// Served as-is (no rebuild needed to change). Same Supabase project as scheduler/VSBE.
// Fill these in for local dev / on the deployed Netlify site.
window.__WB_HUB_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  LINKS: {
    scheduler: "",
    vsbe: "",
  },
};
